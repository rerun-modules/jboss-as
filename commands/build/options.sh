# Generated by stubbs:add-option. Do not edit, if using stubbs.
# Created: Wed Oct 31 09:19:39 PDT 2012
#
#/ usage: jboss-as:build  --directory|-d <> [ --format|-f <rpm>] [ --release|-r <1>] [ --url|-u <http://download.jboss.org/jbossas>]  --version|-v <> 

# print USAGE and exit
rerun_option_usage() {
    grep '^#/ usage:' <"$0" | cut -c4- >&2
    return 2
}

# print SYNTAX and exit
rerun_option_error() {
    if [[ "$RERUN_COLOR" == "true" ]]
    then echo >&2 -e """SYNTAX: $*"""
    else echo >&2 "SYNTAX: $*" 
    fi
    exit 2
}

# check option has its argument
rerun_option_check() {
    [ "$1" -lt 2 ] && rerun_option_usage
}

# options: [directory format release url version]
while [ "$#" -gt 0 ]; do
    OPT="$1"
    case "$OPT" in
        --directory|-d) rerun_option_check $# ; DIRECTORY=$2 ; shift ;;
        --format|-f) rerun_option_check $# ; FORMAT=$2 ; shift ;;
        --release|-r) rerun_option_check $# ; RELEASE=$2 ; shift ;;
        --url|-u) rerun_option_check $# ; URL=$2 ; shift ;;
        --version|-v) rerun_option_check $# ; VERSION=$2 ; shift ;;
        # help option
        -?)
            rerun_option_usage
            exit 2
            ;;
        # end of options, just arguments left
        *)
          break
    esac
    shift
done

# If defaultable options variables are unset, set them to their DEFAULT
[ -z "$FORMAT" ] && FORMAT="rpm"
[ -z "$RELEASE" ] && RELEASE="1"
[ -z "$URL" ] && URL="http://download.jboss.org/jbossas"
# Check required options are set
[ -z "$DIRECTORY" ] && { echo >&2 "missing required option: --directory" ; return 2 ; }
[ -z "$VERSION" ] && { echo >&2 "missing required option: --version" ; return 2 ; }
# If option variables are declared exportable, export them.

#
return 0
